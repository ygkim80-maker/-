# 저장소 작업 규칙

이 저장소는 여러 독립적인 작업(프로젝트)을 브랜치 단위로 진행합니다.
각 브랜치의 산출물이 나중에 `main`으로 합쳐져도 서로 충돌하지 않도록,
아래 규칙을 반드시 지킵니다.

## 새 작업은 반드시 전용 폴더 안에서 진행

- 저장소 루트에 파일을 직접 만들지 않습니다 (`README.md`, `backend/`, `src/` 등을
  루트에 바로 생성 금지). 루트는 여러 프로젝트 폴더를 담는 용도로만 씁니다.
- 새 작업을 시작할 때 작업 내용을 설명하는 kebab-case 폴더를 루트에 하나 만들고,
  그 안에서만 코드/문서를 작성합니다. 예: `hunet-ai-learning-benchmark/`.
- 폴더 이름은 브랜치 이름의 무작위 접미사(`-pgmz4g` 등)를 뺀, 작업 내용을 설명하는
  이름으로 짓습니다.
- 해당 작업의 README, 실행 방법 등 모든 문서도 그 프로젝트 폴더 안에 둡니다.

## 예시 구조

```
/
  hunet-ai-learning-benchmark/   # 프로젝트별 폴더
    README.md
    backend/
  (다음 작업)-폴더명/
    ...
```
